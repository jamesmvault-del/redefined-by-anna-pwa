# Agent Guardrails

This is a premium repsonsive pwa for a therapist client. The name of the business is "Redefined By Anna". Anna is the sole therapist for this business so language implemented into the pwa that is public facing must take this into consideration as if Anna is relaying the infomration herself.. so no "our" or "we" language. The website must not have a templated look and must not use a standard card layout.. The site must use photorealictic images as the ui design and must have an editorial premium resposive layout. The pwa must look like a premium website and look great for both desktop and mobile view.

You have the ability to generate photorealistic and vector style images via API key and script in the /scripts folder, and must use this to complete and maintain design aesthetics when necessary.

These rules override default behaviour. If any rule conflicts with typical coding practices, follow these rules.

1. Fully understand the user request before making any changes.
2. Modify only the logic required to implement the request.
3. Preserve all existing behaviour unless the user explicitly requests changes.
4. Preserve all existing styling and aesthetic unless the user explicitly requests changes.
5. Never assume missing information. Inspect relevant files first.
6. Prefer the simplest working solution. DO NOT over-engineer. DO NOT over-complicate.
7. Extend existing logic before creating new systems.
8. Limit edits strictly to the file and logic required for the request.
9. Work on one file at a time. Multiple files may be created, but only sequentially.
10. Never rewrite an entire file unless the user explicitly requests a full rewrite.
11. All public facing information for this pwa must be human readable in a personal and thoughtful tone.
12. Keep file structure professional and easy to maintain.
13. Any updates must be updated and work correctly for both desktop and mobile views unless explicitly requested otherwise.
14. Currnet and new Animations must be cohesive across all pages and preserved unless the user explicitly requests otherwise.
